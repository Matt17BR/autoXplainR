# AutoXplainR 0.3.0 release evidence

Prepared 2026-09-07. This record covers the release source, not CRAN acceptance.

| Check | Local result |
|---|---|
| R runtime | 4.5.2, Ubuntu 26.04.1, x86_64 |
| Complete installed-engine suite | 1,915 passes; zero failures/warnings; two opt-in live skips |
| Live H2O suite | 93 passes; zero failures/warnings/skips |
| Statement coverage | 89.28%; see `results/coverage.txt` |
| Lint and spelling | Clean |
| Documentation | Both vignettes and pkgdown site built |
| PDP reference agreement | Maximum absolute difference from iml below 3e-14 in all four scenarios |
| Bootstrap coverage experiment | 90% at n=20 and 96% at n=100; 100 replicates per setting, nominal 95% |
| Browser review | Desktop and 390px mobile; no page-level horizontal overflow; print styles inspected |

The complete suite includes every optional native engine installed locally.
The live Gemini test was not run. Mocked transport tests pass, but current hosted
provider availability is not certified. H2O was separately exercised on a local
Java cluster. Coverage percentages are line/statement instrumentation results,
not the percentage of statistical behaviors proven correct.

The small bootstrap experiment shows undercoverage in one setting; it is retained
as a limitation. Reproduce it with `Rscript validation/run-simulation.R`. Reproduce
reference comparisons with `Rscript validation/run-reference.R`; environment
versions are in `results/reference-session.txt`.

The example HTML uses public synthetic data and is generated by
`validation/render-example.R`. Keyboard focus and responsive layouts were
inspected, but this is not a completed WCAG conformance audit or screen-reader
certification.

## Source and publication

The final GitHub tag identifies the source commit. The release workflow attaches
the checked archive, `SHA256SUMS` and the version-specific release notes. Main
branch checks must succeed before the version tag is pushed. The release
workflow then gates publication on native tests, coverage, lint, reference
comparisons, the live H2O integration, and checks of the same source archive
under release R and R-devel.

Local archive check details and remote run links are appended below when available.
Older Win-builder results are deliberately not reused. CRAN submission is a
separate maintainer action and is not performed by the GitHub release workflow.
