# Final public report walkthrough

The root agent used the actual in-app browser after the strict HTTP verifier
confirmed all eleven public assets matched reviewed source `99de419c` and main
`61aaa8f9`. These are executed agent tasks, not a recruited-participant study.
The earlier `34813b11` walkthrough remains a separate observation.

- **Comparison and alternatives:** the delivery report exposes the named primary
  comparison group, with linear-model RMSE 3.00 hours, baseline 7.565, difference
  -4.564 and paired interval -5.621 to -3.465. Selecting training-time cost then
  opening Feature effects retained that choice in URL state. The named linear
  importance group exposed five controls. Selecting Neural network changed the
  named group and actual values, including planned-route importance +2.415 and
  distance importance +1.82. Its model dialog showed one hidden unit, decay 0.1,
  2,000 maximum iterations, eight weights and convergence code 0. Escape closed
  the dialog and restored focus to the model-details link.
- **Missing inputs:** the Data tab identified all 360 embedded original records.
  Filtering `parcel_kg` for missing values returned exactly source rows 17, 39,
  112, 206 and 283. Selecting row 17 showed its retained training identity and
  the raw missing value beside processed value 1.7, with the other raw and
  processed values still present.
- **Binary decisions at phone width:** at a 390 by 844 viewport, the public churn
  report showed all 48 test rows and event `yes`. Keyboard End moved the cutoff
  from 0.50 to 1.00. False positives changed from 3 to 0, false negatives from
  8 to 13, accuracy to 72.9%, and precision to Not defined. The confusion table
  showed 35 true negatives and 13 false negatives. The actual screenshot was
  inspected; the document's client and scroll widths were both 375 pixels.
  The temporary viewport override was reset afterward.
- **Multiclass interpretation:** changing the curve class from setosa to
  virginica updated the class label and effect values. Adding Decision tree
  showed both model settings and the two curves on shared axes. The neural
  curve ranged approximately -0.178 to +0.326; the tree from -0.2444 to +0.4889.
  The actual desktop comparison screenshot was inspected for legibility.

The inspected regression, binary and multiclass pages reported no browser
warnings or errors. Hash verification binds the served assets separately;
these tasks do not claim exhaustive accessibility or statistical validation.
