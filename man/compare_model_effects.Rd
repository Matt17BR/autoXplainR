% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_effect_comparison.R
\name{compare_model_effects}
\alias{compare_model_effects}
\title{Compare one fitted feature effect across retained models}
\usage{
compare_model_effects(
  result,
  feature,
  models = NULL,
  method = c("ale", "pdp"),
  class = NULL,
  n_points = 20L,
  quantile_range = c(0.05, 0.95),
  sample_size = 1000L,
  seed = 123L,
  min_support = 0.1,
  performance_tolerance = NULL
)
}
\arguments{
\item{result}{An \code{autoxplain_result} containing at least two retained,
non-baseline models.}

\item{feature}{One predictor name shared by the retained explainers.}

\item{models}{Model IDs, indices, or \code{NULL}. \code{NULL} selects retained
non-baseline models.}

\item{method}{\code{"ale"} or \code{"pdp"}.}

\item{class}{For multiclass results, the probability class to compare.
Defaults to the first outcome level.}

\item{n_points}{Number of ALE bins or PDP grid points.}

\item{quantile_range}{Numeric length-two range for a numeric PDP grid.}

\item{sample_size}{Maximum common evaluation rows used for PDP. \code{NULL} uses
all rows.}

\item{seed}{Reproducible PDP sampling seed.}

\item{min_support}{Minimum relative empirical support, from zero to one, for
a grid point to enter pairwise summaries.}

\item{performance_tolerance}{Optional non-negative relative performance gap
from the best selected model.}
}
\value{
An \code{autoxplain_model_effects} object containing aligned \code{curves},
pairwise \code{comparisons}, model performance context, failures, findings, and
interpretation scope. \code{n_evaluation_rows} counts the common evaluation
set; \code{n_effect_rows} counts rows that actually entered each effect estimate
after reproducible PDP sampling or ALE missing-value exclusion. \code{models}
retains every requested model with \code{effect_status} and \code{effect_error}.
}
\description{
\code{compare_model_effects()} evaluates the same feature, prediction target,
evaluation rows, and effect grid for every selected model. It is the
feature-level companion to \code{\link[=compare_model_behavior]{compare_model_behavior()}}: use it when the
question is not only whether models predict differently, but how their
fitted response to a named input differs.
}
\details{
ALE remains the default for numeric features. Categorical features require
\code{method = "pdp"}. Comparisons are restricted to grid points meeting
\code{min_support} for both models. PDP dependence diagnostics remain attached;
curves are descriptive properties of fitted prediction functions, not
intervention effects or population confidence bands.

Pairwise columns have deliberately narrow meanings:
\itemize{
\item \code{overlap} is the supported numeric grid span or supported categorical
levels shared by the pair; \code{n_overlap} is its number of grid points.
\item \code{mean_absolute_level_gap} is the mean absolute prediction-scale gap and
therefore includes vertical offsets between curves.
\item \code{centered_shape_rmse} is the root mean squared gap after centering each
curve on its own supported mean. It describes shape disagreement and is
undefined with fewer than two shared points.
\item \code{max_absolute_gap} is the largest prediction-scale gap.
\item \code{effect_rank_correlation} is Spearman correlation between paired effect
values. \code{direction_agreement} is the fraction of adjacent numeric grid
intervals in which the two curves change in the same direction.
}

Effect bands are descriptive, conditional on each already-fitted model.
They summarize across-row variation for PDP or propagated within-bin local
difference variation for ALE; they do not include model-fitting uncertainty.
}
\examples{
fit <- autoxplain(mtcars, "mpg", model_set = "comparison", seed = 2026)
compare_model_effects(fit, "wt")
}
