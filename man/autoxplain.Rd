% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoxplain.R
\name{autoxplain}
\alias{autoxplain}
\title{Fit and evaluate a model through a guided workflow}
\usage{
autoxplain(
  data,
  target_column,
  max_models = 10L,
  max_runtime_secs = 300L,
  seed = 123L,
  test_data = NULL,
  test_fraction = 0.2,
  engine = c("auto", "base", "h2o"),
  enable_preprocessing = TRUE,
  preprocessing_config = list(),
  task = c("auto", "regression", "binary", "multiclass"),
  nfolds = 5L,
  sort_metric = "AUTO",
  include_algos = NULL,
  exclude_algos = NULL,
  use_test_as_validation = FALSE,
  init_h2o = TRUE,
  h2o_nthreads = -1L,
  h2o_max_mem_size = "2G",
  verbosity = c("quiet", "info")
)
}
\arguments{
\item{data}{Training data frame.}

\item{target_column}{Name of the outcome column.}

\item{max_models}{Maximum number of H2O base models; ignored by the guided
base engine, which fits one primary model and one simple baseline.}

\item{max_runtime_secs}{H2O training time budget in seconds; ignored by the
guided base engine.}

\item{seed}{Reproducible split, fitting, and H2O seed.}

\item{test_data}{Optional held-out evaluation data. It is not used as an H2O
validation frame unless \code{use_test_as_validation = TRUE}.}

\item{test_fraction}{Fraction of \code{data} reserved for evaluation when
\code{test_data} is not supplied. Classification splits are stratified.}

\item{engine}{One of \code{"auto"}, \code{"base"}, or \code{"h2o"}. \code{"auto"} currently
resolves to the dependency-free \code{"base"} workflow.}

\item{enable_preprocessing}{Apply \code{\link[=preprocess_data]{preprocess_data()}}.}

\item{preprocessing_config}{Named overrides for preprocessing. Identifier
removal defaults to \code{FALSE}.}

\item{task}{One of \code{"auto"}, \code{"regression"}, \code{"binary"}, or
\code{"multiclass"}.}

\item{nfolds}{Number of H2O cross-validation folds. At least two folds are
required when stacked ensembles are desired.}

\item{sort_metric}{H2O AutoML leaderboard metric.}

\item{include_algos, exclude_algos}{Optional H2O algorithm filters. Supply at
most one.}

\item{use_test_as_validation}{Whether to pass \code{test_data} to H2O as a
validation frame. The default keeps held-out explanation data independent.}

\item{init_h2o}{Start a local H2O cluster when no connection is available.}

\item{h2o_nthreads}{Threads used when starting H2O.}

\item{h2o_max_mem_size}{Memory used when starting H2O.}

\item{verbosity}{One of \code{"quiet"} or \code{"info"}.}
}
\value{
An \code{autoxplain_result} containing models, a data-frame leaderboard,
task metadata, preprocessing provenance, and evaluation data.
}
\description{
\code{autoxplain()} is the beginner-first entry point. By default it creates a
reproducible held-out split, fits an intercept-only baseline and an
understandable statistical model, evaluates both on unseen rows, and stores
everything needed for explanation and reporting. This path only uses R and
does not require Java or a cloud account.
}
\details{
Set \code{engine = "h2o"} to use the optional H2O AutoML adapter. The lower-level
\code{\link[=explain_model]{explain_model()}} interface accepts models fitted by any framework.

Numeric outcomes with exactly two distinct values are treated as binary
classification by default. Potentially destructive preprocessing, such as
identifier removal, is opt-in and recorded in the result.
}
\examples{
result <- autoxplain(mtcars, "mpg")
result
explainers <- as_explainers(result)
audit_explanations(explainers)

\dontrun{
h2o_result <- autoxplain(mtcars, "mpg", engine = "h2o", max_models = 3)
}
}
