% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explainer.R
\name{explain_model}
\alias{explain_model}
\title{Create a model-agnostic explainer}
\usage{
explain_model(
  model,
  data,
  y,
  predict_function = NULL,
  task = c("auto", "regression", "binary", "multiclass"),
  label = NULL,
  positive = NULL,
  metadata = list()
)
}
\arguments{
\item{model}{A fitted model object.}

\item{data}{A data frame. It may contain the outcome when \code{y} is the name of
a column; the outcome is removed from the feature data stored in the
explainer.}

\item{y}{Outcome values, or a single column name in \code{data}.}

\item{predict_function}{Optional prediction function. It may have signature
\verb{function(model, newdata)} or \verb{function(newdata)}. Regression functions
should return a numeric vector. Classification functions should return
probabilities (a vector for binary outcomes or a matrix/data frame with
one column per class).}

\item{task}{One of \code{"auto"}, \code{"regression"}, \code{"binary"}, or
\code{"multiclass"}.}

\item{label}{Human-readable model label.}

\item{positive}{Positive outcome level for binary classification. By
default the second outcome level is used.}

\item{metadata}{Optional named list recorded in the explainer provenance.}
}
\value{
An object of class \code{autoxplain_explainer}.
}
\description{
\code{explain_model()} defines the prediction contract used by AutoXplainR.  It
deliberately separates model fitting from explanation, so base R models,
H2O models, and models from other frameworks can be audited in the same way.
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
explainer <- explain_model(fit, mtcars, y = "mpg", label = "linear model")
explainer
}
