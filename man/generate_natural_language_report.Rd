% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_integration.R
\name{generate_natural_language_report}
\alias{generate_natural_language_report}
\title{Summarize retained model evidence in prose}
\usage{
generate_natural_language_report(
  autoxplain_result,
  importance_data = NULL,
  pdp_data = NULL,
  model_characteristics = NULL,
  audit = NULL,
  provider = c("local", "gemini", "groq", "cloudflare", "ollama", "openrouter", "custom"),
  api_key = NULL,
  model = NULL,
  base_url = NULL,
  account_id = NULL,
  max_tokens = 4000L,
  temperature = NULL,
  timeout = 30,
  structured = TRUE,
  fallback = TRUE,
  use_remote = NULL,
  transport = NULL
)
}
\arguments{
\item{autoxplain_result}{An \code{autoxplain_result} or \code{autoxplain_audit}.}

\item{importance_data}{Optional permutation importance table overriding the
retained audit's importance component. Tables with an explainer identity
must match the primary model. Bare tables are labeled user-supplied with
unverified identity; their metric should be supplied as a \code{metric} attribute.}

\item{pdp_data}{Optional feature-effect list overriding retained effects.
An empty list omits effects from the narrative.}

\item{model_characteristics}{Retained for compatibility. Generic model-family
capacity descriptions are not included in the memo.}

\item{audit}{Optional \code{autoxplain_audit} overriding retained audit findings
and importance. The result's evaluation and effects remain available.}

\item{provider}{One of \code{"local"}, \code{"gemini"}, \code{"groq"},
\code{"cloudflare"}, \code{"ollama"}, \code{"openrouter"}, or \code{"custom"}. The default
is always \code{"local"}.}

\item{api_key}{Provider API key. For an explicitly selected hosted provider,
the corresponding environment variable shown by \code{\link[=narrative_providers]{narrative_providers()}}
is consulted when this is \code{NULL}.}

\item{model}{Model identifier. \code{NULL} uses the provider default shown by
\code{\link[=narrative_providers]{narrative_providers()}}. A custom provider requires an explicit model.}

\item{base_url}{Optional endpoint override. A custom provider requires it.}

\item{account_id}{Cloudflare account ID. When \code{NULL},
\code{CLOUDFLARE_ACCOUNT_ID} is consulted for the Cloudflare provider.}

\item{max_tokens}{Maximum provider generation-token budget. Gemini reasoning
and response text share this budget, so completion is not guaranteed. The
default Gemini model requests low thinking. Generated content is limited
to 500 words; fixed interpretation notes are appended afterward.}

\item{temperature}{Sampling temperature. \code{NULL} uses 1 for Gemini, following
its current model guidance, and 0.2 for other providers.}

\item{timeout}{Request timeout in seconds.}

\item{structured}{Request a validated five-section JSON response when the
selected provider supports it. AutoXplainR renders the validated fields
and adds fixed interpretation boundaries locally. See
\code{\link[=narrative_providers]{narrative_providers()}} for current capability declarations.}

\item{fallback}{Return the deterministic narrative when the remote call
fails.}

\item{use_remote}{Deprecated compatibility switch. \code{FALSE} forces local
generation; \code{TRUE} with no explicit provider selects Gemini. Prefer
\code{provider}.}

\item{transport}{Optional advanced request function for testing or custom
networking. It receives one request list and must return response text;
when \code{request$structured} is \code{TRUE}, that text must be schema-conforming
JSON.}
}
\value{
A single character string with a \code{narrative_provenance} attribute.
}
\description{
Produces a deterministic local narrative by default. Remote or locally
hosted generative models are used only when \code{provider} is set explicitly.
Evaluation, retained explanation findings, fitted effects and failed checks
are included automatically. Prompt construction omits raw rows, fitted
objects and case-level predictions; names and diagnostic text may still be
sensitive. Hosted output is checked for format and length, not numerical
grounding. Review generated claims against the computed evidence.
}
