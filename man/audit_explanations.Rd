% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_explanations}
\alias{audit_explanations}
\title{Stress-test the evidence behind model explanations}
\usage{
audit_explanations(
  explainers,
  features = NULL,
  metric = "auto",
  n_repeats = 20L,
  seed = 123L,
  confidence = 0.95,
  performance_tolerance = 0.05,
  dependence_threshold = 0.7
)
}
\arguments{
\item{explainers}{An \code{autoxplain_explainer} or a list of explainers.}

\item{features}{Features shared by every explainer. Defaults to their
intersection.}

\item{metric}{Performance metric passed to
\code{\link[=calculate_permutation_importance]{calculate_permutation_importance()}}. With \code{"auto"}, explainers carrying
an \code{autoxplain_result} primary metric use that same metric for both
performance screening and permutation importance.}

\item{n_repeats}{Number of permutations per model and feature.}

\item{seed}{Reproducible seed.}

\item{confidence}{Monte Carlo interval level.}

\item{performance_tolerance}{Relative tolerance defining the empirical set
of near-optimal supplied models. For example, \code{0.05} retains models whose
evaluation score is within five percent of the best supplied score.}

\item{dependence_threshold}{Association above which marginal importance and
PDP claims receive a dependence warning.}
}
\value{
An object of class \code{autoxplain_audit}.
}
\description{
\code{audit_explanations()} is AutoXplainR's advanced reliability layer. It evaluates
repeated permutation importance, feature dependence, Monte Carlo stability,
prediction disagreement, and explanation disagreement among near-equivalent
models. The output distinguishes robust descriptive evidence from claims
that need more data or a conditional method.
}
\details{
This is a diagnostic protocol, not a formal certification or a substitute
for domain review, causal identification, or external validation.
}
\examples{
train <- mtcars[1:24, ]
test <- mtcars[25:32, ]
lm1 <- lm(mpg ~ wt + hp + disp, train)
lm2 <- lm(mpg ~ wt + hp + qsec, train)
e1 <- explain_model(lm1, test, "mpg", label = "model A")
e2 <- explain_model(lm2, test, "mpg", label = "model B")
audit <- audit_explanations(list(e1, e2), n_repeats = 5)
audit
}
