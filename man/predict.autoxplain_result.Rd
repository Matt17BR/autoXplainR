% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result_workflow.R
\name{predict.autoxplain_result}
\alias{predict.autoxplain_result}
\title{Predict from an AutoXplainR result}
\usage{
\method{predict}{autoxplain_result}(object, newdata, model = NULL, type = c("response", "class"), ...)
}
\arguments{
\item{object}{An \code{\link[=autoxplain]{autoxplain()}} or \code{\link[=evaluate_models]{evaluate_models()}} result.}

\item{newdata}{Data frame of raw predictor rows.}

\item{model}{One model ID or index. \code{NULL} uses the primary model.}

\item{type}{\code{"response"} returns numeric predictions for regression, positive-
class probabilities for binary tasks, and a named probability matrix for
multiclass tasks. \code{"class"} returns a factor for classification.}

\item{...}{Reserved for future use; additional arguments are rejected.}
}
\value{
A vector or matrix with one prediction per input row. Classification
levels follow the recorded class contract. Binary probabilities refer to
the recorded positive class: the second outcome level for \code{\link[=autoxplain]{autoxplain()}},
or the explicitly chosen event for \code{\link[=evaluate_models]{evaluate_models()}}. Novel categorical
levels follow the stored recipe or supplied prediction contract.
}
\description{
Applies the stored training recipe to raw predictor rows, then predicts with
the pre-specified or training-selected primary model. Existing models brought
through \code{\link[=evaluate_models]{evaluate_models()}} use their recorded prediction contracts, including
any preprocessing inside the supplied predictor. No model is refitted.
The target column is optional and ignored. Row order and row count are kept;
recipes that drop incomplete rows return \code{NA} at those positions.
}
\examples{
result <- autoxplain(mtcars, "mpg", explain = FALSE)
predict(result, mtcars[1:3, c("wt", "hp", "cyl", "disp", "drat",
                             "qsec", "vs", "am", "gear", "carb")])
}
