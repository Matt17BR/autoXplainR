% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\docType{data}
\name{datasets}
\alias{datasets}
\title{Example Datasets for AutoXplainR}
\description{
AutoXplainR includes several example datasets for demonstrating package functionality.
These datasets are stored in \code{inst/extdata/} and cover both classification and
regression tasks with varying complexity.
}
\section{Available Datasets}{


\describe{
  \item{adult_income.csv}{Adult Income Dataset (Classification)
    \itemize{
      \item 48,842 instances with 15 attributes
      \item Target: income (>50K or <=50K)
      \item Features: age, education, occupation, etc.
      \item Use case: Binary classification with mixed data types
      \item Contains missing values marked as "?"
    }
  }
  
  \item{breast-cancer.csv}{Wisconsin Breast Cancer Dataset (Classification)
    \itemize{
      \item 569 instances with 32 attributes
      \item Target: diagnosis (M=malignant, B=benign)
      \item Features: computed from digitized images of breast mass
      \item Use case: Binary classification for medical diagnosis
      \item Clean dataset with no missing values
    }
  }
  
  \item{california-housing.csv}{California Housing Dataset (Regression)
    \itemize{
      \item 20,640 instances with 9 attributes
      \item Target: median_house_value
      \item Features: location, rooms, population, income, etc.
      \item Use case: Regression for price prediction
      \item Geospatial features (latitude, longitude)
    }
  }
  
  \item{pima-diabetes.csv}{Pima Indians Diabetes Dataset (Classification)
    \itemize{
      \item 768 instances with 9 attributes
      \item Target: Outcome (1=diabetes, 0=no diabetes)
      \item Features: medical measurements
      \item Use case: Binary classification for disease prediction
      \item Contains biologically implausible zero values
    }
  }
  
  \item{winequality-red.csv}{Wine Quality Dataset (Regression/Classification)
    \itemize{
      \item 1,599 instances with 12 attributes
      \item Target: quality (score from 0-10)
      \item Features: physicochemical properties
      \item Use case: Can be used for regression or multi-class classification
      \item Imbalanced target distribution
    }
  }
}
}

\section{Loading Datasets}{


\preformatted{
# Load a dataset
data_path <- system.file("extdata", "breast-cancer.csv", 
                         package = "AutoXplainR")
data <- read.csv(data_path, stringsAsFactors = FALSE)

# For datasets with special values (e.g., "?" for missing)
adult_path <- system.file("extdata", "adult_income.csv", 
                          package = "AutoXplainR")
adult_data <- read.csv(adult_path, stringsAsFactors = FALSE, 
                       na.strings = "?")
}
}

\section{Example Workflows}{


\strong{Classification Example:}
\preformatted{
# Load breast cancer data
bc_path <- system.file("extdata", "breast-cancer.csv", 
                       package = "AutoXplainR")
bc_data <- read.csv(bc_path, stringsAsFactors = FALSE)

# Run AutoML
result <- autoxplain(bc_data, "diagnosis", 
                     max_models = 5, 
                     max_runtime_secs = 300)

# Generate dashboard
generate_dashboard(result, "breast_cancer_analysis.html")
}

\strong{Regression Example:}
\preformatted{
# Load housing data (subset for speed)
housing_path <- system.file("extdata", "california-housing.csv", 
                            package = "AutoXplainR")
housing <- read.csv(housing_path, stringsAsFactors = FALSE)
housing_sample <- housing[sample(nrow(housing), 2000), ]

# Run AutoML
result <- autoxplain(housing_sample, "median_house_value",
                     max_models = 3,
                     max_runtime_secs = 180)

# Analyze top features
importance <- calculate_permutation_importance(
  result$models[[1]], 
  housing_sample, 
  "median_house_value"
)
plot_permutation_importance(importance)
}
}

\keyword{datasets}
