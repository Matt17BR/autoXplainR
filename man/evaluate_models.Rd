% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_models.R
\name{evaluate_models}
\alias{evaluate_models}
\title{Evaluate already fitted models on explicitly supplied observations}
\usage{
evaluate_models(
  models,
  data,
  outcome,
  task = c("auto", "regression", "binary", "multiclass"),
  predict_functions = NULL,
  labels = NULL,
  primary = names(models)[[1L]],
  reference = NULL,
  training_data = NULL,
  evaluation_role = "evaluation",
  positive = NULL,
  features = NULL,
  seed = 123L
)
}
\arguments{
\item{models}{A list of fitted objects with unique non-empty model IDs.}

\item{data}{Evaluation observations, including the outcome.}

\item{outcome}{Name of the outcome column.}

\item{task}{Prediction task, or \code{"auto"} to infer it from the outcome.}

\item{predict_functions}{Optional named list of custom prediction functions,
keyed by model ID. Functions take \code{newdata}, or \verb{model, newdata}, as in
\code{\link[=explain_model]{explain_model()}}. They receive the declared \code{features} and own any
preprocessing needed by their fitted model. No recipe is learned here.
Binary numeric outputs must refer to \code{positive}.}

\item{labels}{Optional named character vector of display labels.}

\item{primary}{ID of the user-chosen primary model; defaults to the first ID.}

\item{reference}{Optional ID of an explicitly supplied comparison reference.
It must differ from \code{primary}. No intercept-only model is fitted or inferred.}

\item{training_data}{Optional training observations supplied only as context.
Their use in fitting cannot be verified. \code{NULL} records training unavailable.}

\item{evaluation_role}{User-declared role: \code{"evaluation"} (neutral default),
\code{"test"}, \code{"validation"}, or \code{"training"}. The package cannot verify the role.}

\item{positive}{Binary event label. Defaults to the second declared level.}

\item{features}{Predictor columns; \code{NULL} uses every column except \code{outcome}.
Choose explicitly to keep identifiers or future-only information out of
the model-input and explanation views. Other supplied columns remain local
data context and require explicit \code{context_columns} in \code{\link[=report_data_control]{report_data_control()}}
to enter the report's data explorer.}

\item{seed}{Seed recorded for subsequent explanation randomization.}
}
\value{
An \code{autoxplain_result}. Call \code{\link[=render_model_report]{render_model_report()}} to prepare its
explanations and report. Custom prediction functions are retained for
\code{\link[=predict]{predict()}} and \code{\link[=as_explainers]{as_explainers()}}; required packages and registered prediction
methods must remain available after serialization.
}
\description{
Connects an existing modeling workflow to the same prediction, explanation,
data and report tools as \code{\link[=autoxplain]{autoxplain()}}. No model is fitted, selected, or
replaced. The default evaluation role makes no claim that these rows were
excluded from training or model selection. Classification requires numeric
probabilities, with factor outcome levels declaring the complete class set.
}
\details{
Reusable evidence requires deterministic predictors with explicit model and
lexical state. Referenced closure inputs, custom S3 prediction methods and
supported native formula, offset, contrast and inverse-link extensions are
checked for changes. Arbitrary dynamic lookup, external mutable dependencies
and stochastic prediction callbacks are unsupported; this is not a general
proof of purity for R code. If a model or its assessment inputs change, call
\code{evaluate_models()} again to create a new assessment before reporting.
}
\examples{
fitted <- lm(mpg ~ wt + hp, data = mtcars[1:20, ])
evaluated <- evaluate_models(list(linear = fitted), mtcars[21:32, ], "mpg",
  features = c("wt", "hp")
)
predict(evaluated, mtcars[1:2, ])
}
